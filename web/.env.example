# Nhost Configuration
# Replace with your actual Nhost project URL after creating the project
NHOST_GRAPHQL_URL=https://your-project.nhost.run/v1/graphql

# Authentication (choose one based on your setup)
# Option 1: Use admin secret for development (recommended for development)
NHOST_ADMIN_SECRET=your-admin-secret-here

# Option 2: Use specific role (for production with proper auth)
# NHOST_ROLE=anonymous

# Debugging (set to 1 to enable detailed logs)
DEBUG_SCHEDULE=1

# Application Settings
NEXT_PUBLIC_APP_ENV=development

# Development Notes:
# 1. Copy this file to .env.local for local development
# 2. Set DEBUG_SCHEDULE=1 to see detailed API logs
# 3. The API route will automatically fallback to in-memory storage
#    if Nhost is not available or configured